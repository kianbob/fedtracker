import React from "react";
import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Federal Agencies: Employee Counts & Pay | OpenFeds",
  description: "Browse 128 federal agencies with employee counts, average salaries & workforce data. Compare agencies side by side.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
